<resources>

    <style name="custom_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/theme_white</item>
    </style>

    <style name="dialogProgress" parent="@android:style/Theme.Dialog">
        <item name="android:progressBarStyle">@style/custom_progressbar_style</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="custom_progressbar_style" parent="@android:style/Widget.ProgressBar.Inverse">
        <item name="android:indeterminateDrawable">@drawable/wait_progress_bg</item>
    </style>

</resources>
